class siteController {
    // [GET] /register
    register(req, res, next) {
        return 1;
    }

    // [GET] /account
    account(req, res, next) {
        return 1;
    }

    // [GET] /login
    login(req, res, next) {
        return 1;
    }
}

module.exports = new siteController();
