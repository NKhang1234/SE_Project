class publisherController {
    // [GET] /offer
    offer(req, res, next) {
        return 1;
    }

    // [GET] /offerStatus
    offerStatus(req, res, next) {
        return 1;
    }
}

module.exports = new publisherController();
